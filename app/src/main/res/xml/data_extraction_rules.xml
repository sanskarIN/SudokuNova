<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!--
        Keep SudokuNova's persisted gameplay/learning data out of cloud backup.
        The app provides its own explicit export/restore flow for portable backups.
    -->
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
        <exclude domain="device_root" path="." />
        <exclude domain="device_file" path="." />
        <exclude domain="device_database" path="." />
        <exclude domain="device_sharedpref" path="." />
    </cloud-backup>

    <!-- Preserve ordinary local app state during user-initiated device migration. -->
    <device-transfer>
        <include domain="file" path="." />
        <include domain="database" path="." />
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
